# Import necessary modules
from deephaven import Table

# Select the table
raw_fields_table = dh.table("MIDS", "RawFields")

# Apply the filter and select columns
result_table = raw_fields_table.where("Date == '2023-11-14'", "SymbolId == 'NAV'").view(
    "AcceptedPrice", "AcceptedQuantity", "AccountId", "CancelReason",
    "ClOrdId", "DisplayedOpenQuantity", "ExchangeOrderId", "ExecutionPrice",
    "FirmName", "IsBookOnly", "MatchId", "MessageId", "OrderQty", "PegPrice",
    "Quantity", "RoutedOrderId", "RoutedOrderType", "SymbolSfx",
    "TimeInForce", "TargetCompID"
)

# Display the result
result_table.head()
