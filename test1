from deephaven import init, TableTools

# Initialize Deephaven connection
init()

# Specify the query to select data from the "Orders" table
query = "Orders"

# Execute the query and fetch the result as a Pandas DataFrame
result_df = TableTools.readTable(query).toPandas()

# Print the result
print(result_df)
