import string

# ... Other code ...

def generate_message(message_type, session_name):
    if message_type == 'NewOrderSingle':
        # ... Rest of the code ...

        # Generate a random cl_ord_id with only ASCII printable characters
        cl_ord_id = ''.join(choices(string.ascii_letters + string.digits + string.punctuation, k=20))

        # ... Rest of the code ...
